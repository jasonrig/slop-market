{
  "name": "slop-market",
  "version": "0.1.0",
  "private": true,
  "description": "A Codex-first plugin marketplace scaffold that also supports Claude Code.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "scaffold": "node scripts/scaffold-plugin.mjs",
    "validate": "node scripts/validate-marketplace.mjs",
    "validate:claude-install": "node scripts/validate-claude-installs.mjs",
    "validate:codex-install": "node scripts/validate-codex-installs.mjs",
    "validate:native": "bash scripts/validate-native.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "ajv": "^8.20.0"
  }
}
