{
  "name": "slop-market",
  "version": "0.1.0",
  "private": true,
  "description": "A Codex-first plugin marketplace scaffold that also supports Claude Code.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-marketplace.mjs",
    "validate:native": "bash scripts/validate-native.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
