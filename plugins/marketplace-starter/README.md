# Marketplace Starter

This plugin is intentionally tiny. It verifies that Slop Market exposes a plugin through both the Codex and Claude Code marketplace formats.

Skill:

- `install-check`: confirms that the plugin loaded.

Use it as a reference when adding a real plugin to the marketplace.
